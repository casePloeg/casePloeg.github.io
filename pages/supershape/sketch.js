var increment;
var num_points;

function setup() {
  // put setup code here
    createCanvas(400, 400);
    var num_points = 500
    increment = num_points / num_points
    stroke(255);
    fill(255);
    strokeWeight(4);
    
}

function draw() {
  // put drawing code here
    background(0);
    point(200, 200);
}